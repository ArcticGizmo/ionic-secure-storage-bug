import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-secure-storage-bug',
  webDir: 'dist'
};

export default config;
